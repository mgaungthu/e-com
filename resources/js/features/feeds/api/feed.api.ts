import { api } from "@/api/client";
import type { Feed, FeedPagination, FeedValues } from "@/features/feeds/types/feed.types";
const formData = (values: FeedValues) => { const data = new FormData(); data.append("caption", values.caption); data.append("status", values.status); data.append("is_active", values.is_active ? "1" : "0"); if (values.published_at) data.append("published_at", values.published_at); values.images.forEach((image) => data.append("images[]", image)); values.remove_media_ids.forEach((id) => data.append("remove_media_ids[]", String(id))); values.media_order.forEach((id) => data.append("media_order[]", String(id))); values.products.forEach((product, index) => { data.append(`products[${index}][product_id]`, String(product.product_id)); data.append(`products[${index}][sort_order]`, String(product.sort_order)); }); return data; };
export const feedApi = { list: async (params: object) => (await api.get<{ success: boolean; data: FeedPagination }>("/admin/feeds", { params })).data, show: async (id: number) => (await api.get<{ success: boolean; data: { feed: Feed } }>(`/admin/feeds/${id}`)).data, create: async (values: FeedValues) => (await api.post("/admin/feeds", formData(values))).data, update: async (id: number, values: FeedValues) => { const data = formData(values); data.append("_method", "PATCH"); return (await api.post(`/admin/feeds/${id}`, data)).data; }, remove: async (id: number) => api.delete(`/admin/feeds/${id}`) };
